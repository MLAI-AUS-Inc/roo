# Copy to ".env" and fill in. Never commit the real .env.
# Luma calendar or org API key (Settings -> API; requires Luma Plus)
LUMA_API_KEY=secret_xxx
# Stripe RESTRICTED key, read-only: Charges, Balance transactions, Payouts, Customers
STRIPE_API_KEY=rk_live_xxx
